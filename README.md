# Polycode mainline Windows candidate assets

Pinned bytes from [Windows candidate run 34380813272](https://github.com/Miku0139oao/Polycode/actions/runs/34380813272) (`bcc4eaf5`).

This branch exists so `install-mainline.ps1` can download hash-pinned files without putting 60MB into `main` checkouts. It is not a Preview attestation and not a stable release.

`polycode-bun-windows-x64.gz` and `install.ps1` are reused from the existing `v0.2.1` GitHub release; those hashes are unchanged.
